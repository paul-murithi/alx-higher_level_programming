-- 0x0E. SQL - More queries, Always a name
-- Creates table `force_name` with `id and `name` fields.
CREATE TABLE IF NOT EXISTS force_name (
       id INT,
       name VARCHAR(256) NOT NULL
); 
