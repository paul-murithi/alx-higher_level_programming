-- 0x0D. SQL - Introduction, First table
-- Creates table `first_table` in database from args, if not exists.
CREATE TABLE IF NOT EXISTS first_table (
       id INT,
       name VARCHAR(256)
);
